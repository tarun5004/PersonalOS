# Docker Compose self-hosting direction stub.
# App services will be added after the client and server are implemented.

services:
  mongodb:
    image: mongo:7
    container_name: personal-os-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:

