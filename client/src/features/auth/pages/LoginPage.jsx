import { PlaceholderPage } from '../../../components/shared/PlaceholderPage.jsx';

export default function LoginPage() {
  return (
    <PlaceholderPage
      eyebrow="Authentication"
      summary="The login flow will use HttpOnly cookie-based authentication in its approved phase."
      title="Login"
    />
  );
}

