import { PlaceholderPage } from '../../../components/shared/PlaceholderPage.jsx';

export default function RegisterPage() {
  return (
    <PlaceholderPage
      eyebrow="Authentication"
      summary="The registration flow will create an account and restore the session in its approved phase."
      title="Register"
    />
  );
}

